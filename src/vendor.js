/*This file contain reference to the vendor libraries we are using in this project. tis is use by webpack in prod build
only. a separate bundle for vrndor is useful since it is unlikely to change a often as the application code. so all the 
library we refference hee will be written to vendro.js */

/*eslint-disable no-used-vars */
import fetch from 'whatwg-fetch';